## 原理（存储型 XSS 盲打）

你在前端留言框提交的内容**不会在当前页面回显弹窗**，数据存入数据库；**管理员登录后台查看留言列表时，
恶意代码才会被浏览器执行**，攻击者在前端看不到结果，所以叫**XSS 盲打**（属于存储 XSS）。

页面提示：点提示可以看到后台登录地址：`admin_login.php`，管理员账号密码：`admin / 123456`。

<script>alert(document.cookie)</script>
这个已经注入成功，管理员访问留言板的时候会被窃取到cookie